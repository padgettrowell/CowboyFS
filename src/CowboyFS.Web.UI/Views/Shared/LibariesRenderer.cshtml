@model IEnumerable<CowboyFS.Web.UI.Models.Library>

<ul data-role="listview">
    
    @{
        foreach (var library in Model)
        {
            <li>@Html.ActionLink(library.DisplayName, "Index", new { library = library.LibraryId})</li>
        }
    }
</ul>

